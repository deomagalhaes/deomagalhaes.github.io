<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia Tântrico Interativo</title>
    
    <!-- Tailwind CSS for styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Lucide Icons for vector icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* Using a more specific selector to apply the background gradient and font */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #1a001a; /* Fallback color */
            background-image: linear-gradient(to bottom right, #4c004c, #800040, #b34700);
        }
        /* Custom transition for smoother updates */
        .fade-in {
            animation: fadeIn 0.5s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="text-white">

    <!-- Main Application Container -->
    <div id="app-container">
        <!-- This content will be dynamically generated by JavaScript -->
    </div>

    <script>
        // --- DATA ---
        // Data for the different tantric positions.
        const positions = [
            {
                id: 1,
                name: "Yab-Yum (União Sagrada)",
                sanskrit: "यब्युम्",
                icon: "flower-2",
                description: "A posição fundamental do tantra, representando a união do masculino e feminino divinos.",
                benefits: [
                    "Promove conexão espiritual profunda",
                    "Facilita a respiração sincronizada",
                    "Estimula os chakras do coração",
                    "Permite contato visual constante"
                ],
                technique: "Parceiros sentam-se de frente, com as pernas entrelaçadas. A respiração deve ser lenta e sincronizada. Mantenham contato visual e foquem na energia circulando entre vocês.",
                chakras: ["Coração", "Sacral", "Raiz"],
                duration: "15-30 minutos",
                difficulty: "Iniciante",
                illustration: "🧘‍♀️🧘‍♂️"
            },
            {
                id: 2,
                name: "Maithuna Sagrada",
                sanskrit: "मैथुन",
                icon: "heart",
                description: "Prática de união consciente que transcende o físico para alcançar estados elevados de consciência.",
                benefits: [
                    "Desperta a energia Kundalini",
                    "Harmoniza polaridades internas",
                    "Desenvolve presença mindful",
                    "Fortalece vínculo emocional"
                ],
                technique: "Movimento lento e consciente, mantendo foco na respiração e sensações energéticas. A intenção é de reverência mútua e conexão sagrada.",
                chakras: ["Todos os chakras"],
                duration: "30-60 minutos",
                difficulty: "Intermediário",
                illustration: "💞"
            },
            {
                id: 3,
                name: "Posição do Lótus Duplo",
                sanskrit: "पद्मासन द्विगुण",
                icon: "sun",
                description: "Adaptação da posição de meditação clássica para práticas tântricas de casal.",
                benefits: [
                    "Alinhamento energético dos chakras",
                    "Estabilidade e grounding",
                    "Facilita meditação conjunta",
                    "Promove equilíbrio interior"
                ],
                technique: "Ambos em posição de lótus, de frente um para o outro. Sincronizem a respiração e visualizem energia dourada circulando entre vocês.",
                chakras: ["Raiz", "Sacral", "Plexo Solar"],
                duration: "20-45 minutos",
                difficulty: "Avançado",
                illustration: "🪷"
            },
            {
                id: 4,
                name: "Embrace Shiva-Shakti",
                sanskrit: "शिव शक्ति आलिंगन",
                icon: "moon",
                description: "Representa a dança eterna entre as energias masculina (Shiva) e feminina (Shakti).",
                benefits: [
                    "Equilibra energias opostas",
                    "Intensifica intimidade emocional",
                    "Desenvolve sensibilidade energética",
                    "Cultiva presença no momento"
                ],
                technique: "Abraço prolongado com respiração consciente. Sintam as energias se mesclando e criando harmonia. Permitam que o movimento seja natural e intuitivo.",
                chakras: ["Coração", "Garganta", "Terceiro Olho"],
                duration: "10-25 minutos",
                difficulty: "Iniciante",
                illustration: "🫂"
            },
            {
                id: 5,
                name: "Bandha Conjunta",
                sanskrit: "बन्ध युग्म",
                icon: "star",
                description: "Prática avançada que combina contrações musculares (bandhas) com conexão íntima.",
                benefits: [
                    "Fortalece músculos do core",
                    "Intensifica sensações energéticas",
                    "Melhora controle respiratório",
                    "Eleva consciência corporal"
                ],
                technique: "Contraiam e relaxem os músculos pélvicos em sincronia, mantendo respiração profunda. Foquem na energia subindo pela coluna.",
                chakras: ["Raiz", "Sacral", "Plexo Solar"],
                duration: "15-30 minutos",
                difficulty: "Avançado",
                illustration: "⚡"
            }
        ];

        // --- STATE MANAGEMENT ---
        let selectedPositionId = null;
        let currentIndex = 0;
        const appContainer = document.getElementById('app-container');

        // --- UI TEMPLATES ---

        // Template for the main view (menu)
        function createMainViewHTML(position) {
            return `
                <div class="min-h-screen p-6 fade-in">
                    <div class="max-w-6xl mx-auto">
                        <div class="text-center mb-12">
                            <h1 class="text-5xl font-bold text-white mb-4">🕉️ Guia Tântrico</h1>
                            <p class="text-xl text-rose-200 mb-8">Explore as práticas sagradas do tantra com respeito e consciência</p>
                            <div class="bg-white/10 backdrop-blur-md rounded-2xl p-6 border border-white/20 max-w-3xl mx-auto">
                                <div class="flex items-center justify-center gap-2 mb-4">
                                    <i data-lucide="info" class="w-5 h-5 text-rose-300"></i>
                                    <span class="text-rose-200 font-semibold">Aviso Importante</span>
                                </div>
                                <p class="text-rose-100 text-sm leading-relaxed">O tantra é uma prática espiritual milenar que busca a união entre corpo, mente e espírito. Este guia é educativo e deve ser praticado com respeito, consentimento mútuo e consciência espiritual.</p>
                            </div>
                        </div>

                        <!-- Main position carousel -->
                        <div class="mb-8">
                            <div class="bg-white/10 backdrop-blur-md rounded-3xl p-8 shadow-2xl border border-white/20">
                                <div class="flex items-center justify-between mb-6">
                                    <button onclick="prevPosition()" class="p-3 bg-white/20 rounded-full hover:bg-white/30 transition-colors" aria-label="Posição Anterior">
                                        <i data-lucide="arrow-left" class="w-6 h-6 text-white"></i>
                                    </button>
                                    <div class="text-center flex-1">
                                        <div class="text-8xl mb-4">${position.illustration}</div>
                                        <h2 class="text-3xl font-bold text-white mb-2">${position.name}</h2>
                                        <p class="text-xl text-rose-200">${position.sanskrit}</p>
                                    </div>
                                    <button onclick="nextPosition()" class="p-3 bg-white/20 rounded-full hover:bg-white/30 transition-colors" aria-label="Próxima Posição">
                                        <i data-lucide="arrow-right" class="w-6 h-6 text-white"></i>
                                    </button>
                                </div>
                                <div class="text-center">
                                    <p class="text-rose-100 mb-6 max-w-2xl mx-auto">${position.description}</p>
                                    <button onclick="selectPosition(${position.id})" class="bg-gradient-to-r from-rose-500 to-purple-600 hover:from-rose-600 hover:to-purple-700 text-white font-semibold py-4 px-8 rounded-full transition-all transform hover:scale-105 shadow-lg">
                                        Explorar Esta Posição
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Grid of all positions -->
                        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                            ${positions.map(p => createPositionCardHTML(p)).join('')}
                        </div>

                        <!-- Tantra Principles Section -->
                        <div class="mt-12 bg-white/10 backdrop-blur-md rounded-2xl p-6 border border-white/20">
                            <h3 class="text-2xl font-bold text-white mb-4 text-center">Princípios do Tantra</h3>
                            <div class="grid md:grid-cols-3 gap-6 text-center">
                                <div>
                                    <i data-lucide="flower-2" class="w-8 h-8 mx-auto mb-3 text-rose-300"></i>
                                    <h4 class="text-lg font-semibold text-white mb-2">Consciência</h4>
                                    <p class="text-rose-100 text-sm">Presença total no momento presente</p>
                                </div>
                                <div>
                                    <i data-lucide="heart" class="w-8 h-8 mx-auto mb-3 text-rose-300"></i>
                                    <h4 class="text-lg font-semibold text-white mb-2">Conexão</h4>
                                    <p class="text-rose-100 text-sm">União sagrada entre parceiros</p>
                                </div>
                                <div>
                                    <i data-lucide="star" class="w-8 h-8 mx-auto mb-3 text-rose-300"></i>
                                    <h4 class="text-lg font-semibold text-white mb-2">Transcendência</h4>
                                    <p class="text-rose-100 text-sm">Elevação espiritual através da prática</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            `;
        }

        // Template for a single position card in the grid
        function createPositionCardHTML(position) {
            return `
                <div onclick="selectPosition(${position.id})" class="bg-white/10 backdrop-blur-md rounded-2xl p-6 cursor-pointer hover:bg-white/20 transition-all transform hover:-translate-y-2 border border-white/20">
                    <div class="text-center">
                        <div class="text-4xl mb-3">${position.illustration}</div>
                        <div class="flex justify-center mb-3 text-rose-300">
                            <i data-lucide="${position.icon}" class="w-8 h-8"></i>
                        </div>
                        <h3 class="text-xl font-semibold text-white mb-2">${position.name}</h3>
                        <p class="text-rose-200 text-sm mb-4">${position.sanskrit}</p>
                        <div class="flex justify-center gap-2 mb-4">
                            <span class="bg-white/20 px-3 py-1 rounded-full text-white text-xs">${position.difficulty}</span>
                            <span class="bg-white/20 px-3 py-1 rounded-full text-white text-xs">${position.duration}</span>
                        </div>
                        <p class="text-rose-100 text-sm">${position.description.substring(0, 80)}...</p>
                    </div>
                </div>
            `;
        }
        
        // Template for the detailed position view
        function createDetailViewHTML(position) {
            return `
                <div class="min-h-screen p-6 fade-in">
                    <div class="max-w-4xl mx-auto">
                        <button onclick="goBackToMenu()" class="flex items-center gap-2 text-white mb-6 hover:text-rose-200 transition-colors">
                            <i data-lucide="arrow-left" class="w-5 h-5"></i>
                            Voltar ao menu
                        </button>
                        <div class="bg-white/10 backdrop-blur-md rounded-3xl p-8 shadow-2xl border border-white/20">
                            <div class="text-center mb-8">
                                <div class="text-6xl mb-4">${position.illustration}</div>
                                <h1 class="text-4xl font-bold text-white mb-2">${position.name}</h1>
                                <p class="text-2xl text-rose-200 mb-4">${position.sanskrit}</p>
                                <div class="flex justify-center gap-4 mb-6">
                                    <span class="bg-white/20 px-4 py-2 rounded-full text-white text-sm">${position.difficulty}</span>
                                    <span class="bg-white/20 px-4 py-2 rounded-full text-white text-sm">${position.duration}</span>
                                </div>
                            </div>
                            <div class="grid md:grid-cols-2 gap-8">
                                <div class="space-y-6">
                                    <div>
                                        <h3 class="text-xl font-semibold text-white mb-3">Descrição</h3>
                                        <p class="text-rose-100">${position.description}</p>
                                    </div>
                                    <div>
                                        <h3 class="text-xl font-semibold text-white mb-3">Técnica</h3>
                                        <p class="text-rose-100">${position.technique}</p>
                                    </div>
                                    <div>
                                        <h3 class="text-xl font-semibold text-white mb-3">Chakras Ativados</h3>
                                        <div class="flex flex-wrap gap-2">
                                            ${position.chakras.map(chakra => `<span class="bg-gradient-to-r from-purple-500 to-pink-500 px-3 py-1 rounded-full text-white text-sm">${chakra}</span>`).join('')}
                                        </div>
                                    </div>
                                </div>
                                <div>
                                    <h3 class="text-xl font-semibold text-white mb-3">Benefícios</h3>
                                    <ul class="space-y-2">
                                        ${position.benefits.map(benefit => `
                                            <li class="flex items-start gap-2 text-rose-100">
                                                <i data-lucide="heart" class="w-4 h-4 mt-1 text-rose-300 flex-shrink-0"></i>
                                                ${benefit}
                                            </li>
                                        `).join('')}
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            `;
        }

        // --- LOGIC & RENDERING ---

        // Main render function to update the UI
        function render() {
            if (selectedPositionId === null) {
                const currentPosition = positions[currentIndex];
                appContainer.innerHTML = createMainViewHTML(currentPosition);
            } else {
                const position = positions.find(p => p.id === selectedPositionId);
                appContainer.innerHTML = createDetailViewHTML(position);
            }
            // After updating the DOM, tell Lucide to create the icons
            lucide.createIcons();
        }

        // --- EVENT HANDLERS ---
        
        window.nextPosition = () => {
            currentIndex = (currentIndex + 1) % positions.length;
            render();
        };

        window.prevPosition = () => {
            currentIndex = (currentIndex - 1 + positions.length) % positions.length;
            render();
        };

        window.selectPosition = (id) => {
            selectedPositionId = id;
            render();
        };

        window.goBackToMenu = () => {
            selectedPositionId = null;
            render();
        };

        // --- INITIALIZATION ---
        // Initial render when the page loads
        document.addEventListener('DOMContentLoaded', render);

    </script>
</body>
</html>
